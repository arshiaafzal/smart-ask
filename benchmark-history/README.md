# Historical benchmark evidence

This directory contains immutable snapshots from benchmark implementations that
predate the current artifact schema. They are retained for provenance only and
must not be loaded by the live benchmark reader or compared as current runs.

Each dated snapshot documents its own completeness and interpretation limits.
